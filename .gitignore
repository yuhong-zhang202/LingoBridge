# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev.log

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# eval 跑分产物（每跑一次堆一个带时间戳文件；保留 scripts/eval/golden 金标集与 run-extraction.ts）
/scripts/eval/results/

# LLM 原始输出留存（LLM_RAW_LOG_DIR 指向此目录；含用户故事原文，永不入库）
/.llm-raw/

# next-pwa 构建产物
public/sw.js
public/sw.js.map
public/workbox-*.js
public/worker-*.js
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Claude Code 个人本地覆盖（团队共用的在 .claude/settings.json）
.claude/settings.local.json

# 金标迁移备份，不入库
*.bak

# 本地临时/工作产物与个人笔记，不入库（B13 · 产品方 2026-07-25 拍板）
/vercel/
临时/
准确率/
头像/

# ── 设计稿 / 原型 / 素材，不入库（2026-08-13 清出 19.4MB 后立此闸）────────────
# 这三处曾把 19.4MB 非产品文件带进仓库（src/ 全部才 3.5MB），且 网站/ 里还躺过
# 一整个嵌套的 Next.js demo app。设计稿与原型请放仓库【目录之外】。
网站/
design-reference/
UI /
# vercel/：Vercel 时代遗留，内含账号恢复码（secret）。项目已改用 Zeabur。
# ⚠️ gitignore 只挡 git，挡不住打包 / 网盘同步 / git add -f —— 该目录应移出仓库目录。
vercel/
