# 议题台账（DISCUSSION_LOG）

## 这个台账是什么

本文件是 LingoBridge eval loop 的**争议与决策总账**。每一轮 loop 里,任何角色(金标标注、指标、AI 评审、产品、工程)发现的**说不清的地方**都往这里记一条,而不是各自在自己的报告里自说自话。

它记的是**分歧和待定**,不是任务清单。一条进来的标准是:有人认为这里可能有问题,且这个问题不是靠一个人埋头改代码就能消掉的。

## 状态字段谁能改(硬性)

| 状态 | 含义 | 谁能写 |
|---|---|---|
| 待议 | 已入账,未拍板 | 任何角色转条目时,由记录员写入 |
| 已决策 | 人已拍板方向,可能尚未落地 | **只有人**拍板后,记录员据实登记 |
| 已解决 | 讨论结束、落地完成、正式结案 | **只有人**。记录员、AI 角色一律不得自行改写 |
| 已作废（依据来自污染数据） | 条目的**实测依据**读自已被判定污染的导出,不足以支撑原结论 | **只有人**。产品方 2026-07-16 启用此状态。**作废的是依据,不是说结论已被证伪**——需管道修好并重跑全量基线后重估 |

记录员不对条目发表判断、不提解法、不结案。AI 角色报"我修好了"**不等于**已解决——代码修完只是事实,结不结案是人的决定。

## 怎么用

1. 发现争议 → 转给记录员 → 追加一行,状态默认「待议」。
2. 同一问题多角色报告 → 合并为一条,来源列写全,不重复开号。
3. 每轮 loop 结束 → 记录员出「本轮待讨论摘要」:新增几条、按类型分组、优先级排序(只排序,不下结论)。
4. 人拍板 → 记录员把状态改「已决策」或「已解决」,并在条目里留下拍板日期与拍板人。

类型七选一:**合并记录** / 金标争议 / 指标争议 / 产品取舍 / AI稳定不同意金标 / 跨环节风险 / 其他。

## 规则变更(2026-07-16):每次合并 = 台账一条

**「合并记录」类型自 2026-07-16 起启用,由产品方定规则。** 此前台账只记"报上来的争议",不记"做了的事"——结果 `rawCall` / `checkAlignment` / `validate` 这些改动全部没进台账,因为在执行者眼里它们是"实现细节"。**但 validate 决定用户会不会看到模型捏造的维度。**

> **争议只是子集。台账要能重建"这周发生了什么",而不是"这周吵了什么"。**

每一次合并都要记一条,最少三行:(1) 改了什么(**文件:行号级别**,不是"优化了重排");(2) 为什么(解决什么问题 / 依据哪次拍板);(3) 影响哪些用户可见行为(**没有就写"无",但必须显式写**)。

合并记录的状态说明:代码已合并只是**事实**,不等于结案。故一律记「待议(代码已合并,待人确认结案)」,沿用条目 009 的先例——AI 角色报"我修好了"不等于已解决。

---

## 台账

| 编号 | 日期 | 来源 | 环节 | 类型 | 问题描述 | 为什么可疑 | 临时处理 | 状态 |
|---|---|---|---|---|---|---|---|---|
| 001 | 2026-07-16 | 产品方 | 口径定义 | 产品取舍 | 「高匹配」的语义定为:这道题**能原样用语料回答**,而非"最接近的题"。 | 此前"高匹配"在重排、闸门、金标三处口径不一,导致"该收紧还是该放宽"的争论没有共同基准。 | 产品方 2026-07-16 拍板采纳。首屏可用率诚实值 15%(6/40),天花板已满;闸门口径按此定;后续收紧/放宽之争以此为准绳。 | 已决策 |
| 002 | 2026-07-16 | 产品方 / 工程 | 前端渲染 | 产品取舍 | `highGroup=0 且 midGroup>0` 时三个空态判断全部落空:高匹配块不渲染、expanded 初值 false 故中低不渲染、noneVisible 因 mid 非空为 false、globalNoneVisible 因 totalVisible>0 为 false。用户看到大标题「匹配到 N 道当季真题」+ 零张卡 + 一个「查看更多 N 道 →」按钮——标题说有题,屏幕上没题。 | 标题与画面直接矛盾,属客观缺陷,与任何产品取舍无关。且它是"收紧重排"的前置:不修它,收紧会把 12 个故事推进这个洞,影响面从 10 扩到 22。 | 产品方 2026-07-16 拍板:必修。影响面为今日线上 10 个故事(S020/S047/S060/S083/S090/S091/S093/S094/S095/S096)。代码位置 src/app/matching/page.tsx:93-118 + src/app/matching/MatchingMobile.tsx:153-216。 | 已决策 |
| 003 | 2026-07-16 | 产品方 / AI评审 | 重排打分 | 产品取舍 | 4 条客观错(S044/S080/S092/S067):AI 在自己的理由里已写明不符(「需换主语」/「未体现'他人帮助'」/「非'读过的故事'」),却仍给 88-90 分。 | 定性为**分数与理由脱钩**,不是判断力问题——模型已经识别出不符,分数没跟着走。 | 产品方 2026-07-16 拍板:必修。拟用极窄规则:AI 理由自认不符时,分数不得进 ≥85;**具体修复方案待人确认后实施**。注意:这 4 条的全池金标最佳分别是 低/低/中/低,修对之后这 4 个故事首屏会变空,而非变成一张对的卡。**2026-07-16 产品方拍板作废**:S044/S080/S092/S067 的"客观错"认定建立在可能被 014 错位污染的 score+reason 配对上(见 015)。**作废的是依据,不是说这 4 条结论已被证伪**——管道修好并重跑全量基线后重估。 | 已作废（依据来自污染数据） |
| 004 | 2026-07-16 | 金标标注 | 金标 | 金标争议 | 同一道题(questionId 30aa1ea5-8882-4dfc-9130-cbcfcbe4e6d0「你有没有过做得很出色的经历?」)、同一观察点「一次靠死磕做成的事」、结构几乎一样的压力故事,金标一个判**低**(S016)、一个判**中**(S017)。 | 说明"高/中"这条线本身模糊,是真分歧,不是 AI 的错。金标自身不自洽时,任何以金标为准的指标都会带噪。 | 未处理,保留原判,待人讨论。 | 待议 |
| 005 | 2026-07-16 | 金标标注 / 产品方 | 召回与供给 | 跨环节风险 | 供给缺口:题材系统性错配。40 个故事覆盖 19 个观察点,其中 **14 个观察点池内金标高数=0**。6 个"能用"的故事 **100% 落在具象点**(园艺/种植 SPA_08、一件对你特别的物品 SPI_05、自然里让你有感觉的时刻 SPA_07、宠物特殊互动 REL_08、数字世界栖居 SPI_06);零金标高的全是内心题材(压力 EMO_09、被别人帮助 REL_10、坚持原则 VAL_03、冲突 REL_11、失败复盘 GRO_04、想法转变 GRO_05 等)。 | 13 条「AI判高金标非高」里,**10 条 AI 选中的就是全池金标最高的那道**——收紧买不到更好的题,池子里没有。即问题可能不在重排环节,而在供给端。 | 未处理。参照既有判例:docs/匹配链路-权威文档.md:150 REL_06 被定性为「接受的供给缺口」。是否沿用该判例待人定。 | 待议 |
| 006 | 2026-07-16 | 产品方 / 金标标注 | 评测集构造 | 跨环节风险 | 【重要】「产品形态是否成立」的结论待真实数据。这 40 条故事是为**均匀覆盖 48 个观察点合成的**,真实用户的题材分布未知。 | 15% 可用率**只对这批合成故事成立**,不能据此判断产品形态成不成立。任何基于 15% 的战略结论(如"资源转上游")都必须等真实用户数据。 | 未处理。记录员注:本条是 001/005/007 的前提条件。 | 待议 |
| 007 | 2026-07-16 | 产品方 | 题库供给 | 产品取舍 | 题库硬约束:题库是固定的一季真题(scripts/data/question-observation-remap.v2.json 的 basis 字段:雅思口语题库2026年1-4月Version3逐题人工审查),前端文案承诺「当季真题」(MatchingMobile.tsx:115)。**题不能编。** | 「补题库」实质是「要不要放弃'当季'承诺」——雅思整体有这些题(Describe a time you failed 等),但不在这一季。这是承诺层面的取舍,不是工程问题。 | 未处理,承诺文案与题库现状均维持不变,待人定。 | 待议 |
| 008 | 2026-07-16 | 指标 | 红线与指标 | 指标争议 | 召回精度红线有盲区,是否加「金标缺口率」红线。本轮已接入红线 ranking_recall_precision_pct=30.4(口径:本轮召回∩金标已标的对中,金标∈{高,中}/全部计入对;金标视角,方向=不许跌)。盲区:它只看"召回了且金标标过"的对。 | 若映射改动引入大批金标没标过的新题,这些对不入分母 → 指标不动照样 PASS,而漏斗其实已被灌水。即红线可能对一类真实退化完全失明。 | 未自行添加。防守办法是同时守金标缺口率(算分报告 coverage.goldGapRatio,当前 0.0%),但新增红线=改指标定义=需人拍板,故记此待议。 | 待议 |
| 009 | 2026-07-16 | 工程 | 回归守卫 | 其他 | 回归守卫读错文件。regression-all.sh 原用 `latest('scripts/eval/results/ranking-*.json')` 取最新导出,但该 glob 按字母序排,最后一个是 `ranking-v2-scaffold-inherited.json`(金标脚手架,无 summary 键)→ KeyError 崩溃。 | 该脚手架文件 7-15 才出现,故 ranking 的两条检查**从那天起一直没真正校验过**;原基线 ranking_visible_zero_max=10 也因此从未被验证,实测值是 12。即守卫在一段时间内是失效的,期间的 PASS 不可信。 | 代码已修:改用正则只认 ranking-<ISO时间戳>.json;基线已更正为实测值 12。记录员按台账规则未自行结案,待人确认后改「已解决」。 | 待议(代码已修,待人确认结案) |
| 010 | 2026-07-16 | 用户 / AI评审 / 工程 | 重排打分(prompt 规则) | 其他 | 【用户点名:最贵的教训之一】**枚举措辞的规则治不住模型 —— 它会发明新说法绕过。** 案例:2026-07-16 的窄规则列了词表(「需微调」「不是…」「未体现…」「没提到…」「对不上」等),要求"理由里出现这些词就不许给 ≥85"。结果 S067(故事=看剧集《重版出来》,题=「描述一个你最近读过的故事」,金标低)的新理由写的是「…需将'看剧'硬套为'读故事',**属中高匹配但非严格符合**」——它发明了"非严格符合"这个词表里没有的说法,照给 88 分,规则完全落空。 | 词表是**可枚举的封闭集**,模型的表达空间不是。只要判据落在"说了哪几个词"上,模型换一个同义说法即可绕过,规则的约束力等于零——本例是实测证据,不是推演。 | **结论(已采纳):规则要给判据(能不能反复见到 / 主语是谁),不要给词表。** 落地:同日已按此重写为「主语必须对得上」判据式规则(先问"题目要求讲的主角是谁"+"故事里做这件事的是谁"),删除全部词表枚举。 | 已决策 |
| 011 | 2026-07-16 | 用户 / AI评审 / 指标 | 重排打分(prompt 变更) | 其他 | 【用户点名:最贵的教训之一】**改 prompt 有全局副作用,不是精准打击。** 案例:2026-07-16 窄规则只想治 4 条"理由自认不符"的条目,实际 13 条「AI高∩金标非高」里有 5 条发生漂移,且方向不受控:S051 88→**90**(涨)、S084 88→**90**(涨,S084 金标=低,即**新增虚高**)、S075 96→94、S016 94→90、S063 88→82(降)。 | 这是**虚高率不降反升(27.3%→27.8%)的原因之一**:治好的虚高被新冒出来的虚高填平。总分持平/微动会掩盖"一批修好+一批新坏"的真实结构,用总分结案会把失败读成成功。 | 结论:改 prompt 必须预注册预期 + 逐条核对目标条目与非目标条目,不许用总分结案(与 red-team.md 新增铁律同源)。**2026-07-16 产品方拍板作废**:5 条漂移读数(S051/S084/S075/S016/S063)与虚高率 27.3%→27.8% 全部来自已作废导出(见 015)。**作废的是依据,不是说"改 prompt 有全局副作用"已被证伪**——管道修好并重跑全量基线后重估。 | 已作废（依据来自污染数据） |
| 012 | 2026-07-16 | 工程 / 指标 | 回归红线(分母口径) | 指标争议 | 回归红线分母 bug:regression-all.sh 中所有"按故事数算"的指标用了 `len(d['stories'])`(导出里的全部故事)作分母。而 run-ranking.ts 改动后,导出从 40 个故事变成 100 个,金标只覆盖 40 个。后果:首屏可用率 12.5% 被算成 **5.0%**,报了个假 FAIL;「可见=0假空」报 11(100 个里数的)而基线 12(40 个里数的),**两者根本不可比,那个 PASS 是蒙的**(按 40 重算实为 9)。未受影响:召回精度(按 gold∩recalled 算,多出的 60 个故事无金标自动排除)、首屏精确率/虚高率(来自算分报告,本就只对齐金标 40)。 | **衍生认识:按金标对齐的口径天然抗这种规模漂移,按导出规模算的不抗。** 分母随导出规模漂移时,红线会同时产生假 FAIL 与假 PASS,且假 PASS 无声。 | 已修:分母改为「金标覆盖的故事数」,恢复 BASELINE 中写明的 "6/40" 原意。修复已用同一份污染样本验证(5.0%→12.5%,可见=0 假空 11→9),且修复后仍正确 FAIL(因 S074 是真丢),说明没把真问题一起修掉。用户 2026-07-16 明确批准,定性为"修 bug 不是改标准"。 | 已解决 |
| 013 | 2026-07-16 | 用户 / 产品方 / AI评审 | 重排打分(规则范围) | 产品取舍 | 窄规则范围之争。第一版规则(管"理由自认不符"全部情形)的实测后果:目标 4 条修好 3 条(S044 90→0、S080 90→52、S092 88→75),S067 88→88 未修好(见 010);**误伤 S074**——其金标高卡「社交媒体上有趣的事」86→82 掉出高档,新理由「…但未说明'某一次'具体时间/所见细节」,S074 是 6 个"能用"故事之一,首屏可用率 15%→12.5%,回归 FAIL。 | 规则宽窄直接换算成产品可用面:宽规则多修 1 条对虚高率零贡献的中档条目(S092),代价是丢掉 1/6 的可用故事(S074)。 | 用户 2026-07-16 选定 **2b「改窄到只管主语」**,理由:丢 S092 不心疼(金标是中,本就不算虚高,修它对虚高率零贡献);保 S074 很值(占产品可用面 1/6);用两条最硬的客观错(S044/S080 均为主语反转)换零误伤,划算。被否方案:(a) 收回整条规则;(c) 把 usable 基线从 15.0 降到 12.5——用户与主会话一致反对,理由是「拿改考卷来迁就修复」,正是 BASELINE 写死"只有人能改"要防的事。遗留:S067(金标低,仍虚高)与 S092(改窄后预期退回原状)**未修,转入待议**。**2026-07-16 产品方拍板作废**:"修好 3 条 / 误伤 S074 / 可用率 15%→12.5%" 的全部实测后果读自已作废导出(见 015),故 2b 的取舍算式失去数据基础。**作废的是依据,不是说 2b 已被证伪**——管道修好并重跑全量基线后重估。 | 已作废（依据来自污染数据） |
| 014 | 2026-07-16 | 管道审计官 / 主会话 / 产品方 | 重排打分(管道) | 跨环节风险 | 【第4个管道 bug】**UUID join key 错位:模型把整个 {score, reason} 单元贴到错误的 id 上。** 模型返回 `{"scores":[{"id":"<36位UUID>","score":N,"reason":"..."}]}`,管道按 id 查表回填(src/services/matching.ts:171-178),回填逻辑本身正确(按 id 的 Map,非下标对齐)。关键事实:matching.ts:175-176 的 score 与 reason **读自同一个对象 s,是原子的** → 理由错位到哪,分数就一起错位到哪。**不是"只有理由不可信",是分数打给了A题却挂在B题上。** 铁证(均由主会话独立核实):S012(故事:一有压力就整理房间)——a64b6b10(题:你多久休息一次或停下来歇一歇?)拿到 6a65c126(题:如果你明天有空,你最想做什么?)的判词,分数一起互换(78/72),同现象在 **S006/S007/S012/S013/基线S051 共 5 次独立调用**稳定重演;S056——95563aa8(你喜欢拍不同风景的照片吗?)↔ 96570995(你旅行时见过最美丽的景色是什么?)score+reason 整体互换,**两份导出×两个故事=4 次调用全部互换**,跨越两个不同 prompt 版本。 | **系统性可复现、不会被统计抵消**:ranking.ts 用 temperature:0,且 src/lib/db/questions.ts:97-104 取候选无任何 .order() → 输入顺序稳定 → 同一错位稳定重演,是钉在特定题目上的**持久偏置**而非随机噪声。错位**集中在语义相近的题簇**(拍照/景色、休息/明天有空)= "模型分不清该抄哪个 UUID"的指纹。**一直没被发现的原因**:管道对模型返回零校验(matching.ts:171-173:未知 id 静默丢弃、重复 id 静默覆盖、缺失 id 静默留 null),且 llm.ts:132-135 成功调用不留存原始输出 → 错位不可见、也不可复盘。审计官明说:在无原始输出的前提下**给不出错位总量的可信估计**,只能给下界 1.2%。 | 修复已派 fix-engineer(六条):留存原始输出／join key 改短序号 q1/q2／**模型回显题干前缀+校验**／id 集合校验／加 .order('id')／AI 边界 `??` 审计。其中"回显题干前缀"由产品方提出,理由:**单纯的集合校验抓不到互换**(q1/q2 整体对调时集合仍合法)。代码修复不等于结案,待人拍板。 | 待议 |
| 015 | 2026-07-16 | 管道审计官 / 主会话 | 评测结论(全局) | 跨环节风险 | 【结论作废】**因 014 的 join key 错位,2026-07-16 当天基于 ranking 导出得出的全部结论作废,需在管道修好并重跑基线后重估。** 作废范围:虚高率(27.3% / 11.8%)、首屏精确率(40.9% / 52.9%)、4×4 档位混淆矩阵、召回精度、NDCG/τ;13 条"AI判高金标非高"的分诊结论(其中 S056 已确证是错位产物);"S016/S084 是纯供给问题"的定性——它建立在"AI 选中了池里金标最高的题",而配对本身可能是错的;本轮 prompt 修复的全部评价(该改动已于同日 git checkout 收回)。**仍站得住(不依赖分数正确性)**:002 渲染缺陷(纯前端)、005 供给缺口(金标高共 11 道、分布在 6 个故事上——这是金标的事实,与 AI 打分无关)、006 评测集是合成的。 | **本条作废了以下既有条目的依据(记录员未改动其状态,待人处置)**:003(4 条客观错 S044/S080/S092/S067 的认定基于可能错位的 score+reason 配对)、011(5 条漂移与"虚高率 27.3%→27.8%"的读数全部来自作废导出)、013(窄规则范围之争的实测后果——修好 3 条、误伤 S074、可用率 15%→12.5%——全部读自作废导出)、005 与 008 中凡引用"13 条 AI判高金标非高"及召回精度 30.4 的部分。另:**S044/S080 的"修好"存疑**——它们的理由与题面严格对应、看起来没错位,但**"看起来没错位"不是证明**,需重跑确认。 | 未处理。003 的口径调整(原计划改成「S044/S080 必修,S067/S092 转待议」)**暂缓**,待重跑后再定。记录员按台账规则**未改动 003/005/008/011/013 的既有状态**——那是人的权限。 | 待议 |
| 016 | 2026-07-16 | 管道审计官 / 主会话 / 产品方 | 前端渲染(降级策略) | 产品取舍 | 【新线上事故】**`?? 100` 未打分→全标高匹配。** src/app/matching/page.tsx:81/94/99/106 用 `q.relevanceScore ?? 100`,即重排未打分的候选 → 视为 100 分 → ≥85 → 进首屏「高匹配」组。而 src/services/matching.ts:181-182 排序用 `?? -1` 排到最末。**同一字段、两个文件、两个不同默认值**,导致 UI 自相矛盾:未打分题被算进 highGroup 却排在最后。影响面(主会话核实):新导出 100 个故事里 **6 个整体降级**(S005/S022/S030/S048/S049/S078,rankQuestions 返回空数组 → 全部候选 null)→ 这 6/100 的用户会看到**全部候选题都标着「高匹配」且顺序混乱**。 | **与决策 001 直接冲突**:001 拍板「高匹配」= 这道题能原样用语料回答;降级时把全部题标成「高匹配」= 把谎话当容错。口径校准:审计官报的"16.7% 未打分(167/998)"是跨 100 故事的数;**在金标覆盖的 40 个故事内只有 3/293=1.0%,且 0 个整体降级** → 故此事故**影响生产,但不是 015 结论作废的原因**(作废原因是 014 的错位)。 | 未处理。该 `?? 100` 是文档里写明的"优雅降级"(重排失败时保可用性),属产品决策不属纯 bug,**已明确禁止 fix-engineer 改动**;由主会话给方案、产品方拍板。已提交产品方,等方案与拍板。 | 待议 |
| 017 | 2026-07-16 | 管道审计官 / 主会话 | 管道设计(模式教训) | 其他 | 【模式教训】**四个管道 bug 是同一种病,病根一致:`??` 静默塞默认值 + 不校验 AI 返回 + 不留存证据。** 清单:(1) `cue_card_title ?? question_text`(已修);(2) `score ?? -1`(排序)与 `score ?? 100`(分组)——同字段两默认值(见 016);(3) UUID join key 错位 + 零校验(见 014);(4) 成功调用不留原始输出(llm.ts)。 | 关联既有条目 010/011(枚举措辞治不住模型／改 prompt 有全局副作用):本条与它们共同指向同一个更大的教训——**三次都在 prompt 上找原因,这是第四次**(管道审计官原话)。即真因反复落在 AI 边界的数据契约上,而排查反复停在 prompt 层。 | **产品方 2026-07-16 拍板确认**(原话:「那句"凡是跨越 AI 边界的数据必须校验、必须留证、不许用 `??` 兜底"我确认为决策」):凡是跨越 AI 边界的数据,必须校验、必须留证、不许用 `??` 兜底。`??` 在 AI 边界上不是容错,是把错误伪装成正常值。留痕:该处方式推论最初由主会话添加、非产品方原话,记录员曾据台账规则拒绝标「已决策」;现已获产品方明确确认,据实登记。 | 已决策 |
| 018 | 2026-07-16 | 产品方 / 主会话 | 落库(持久化) | 产品取舍 | 【落库层 `?? 100`】src/app/api/matching/route.ts:17 的 `levelForScore` 原为 `const s = score ?? 100`,即重排降级或模型漏题时,`persistMatches` 把全部候选以 `match_level='high'` **永久写进 `corpus_question_matches`**,下游任何读这张表的功能都继承这个谎。 | 与展示层 `?? 100`(见 016)区分:**展示层是瞬时的、是文档写明的产品决策,仍在产品方手上;落库层是持久化的,无辩护理由**。 | 产品方 2026-07-16 拍板:属「我们不知道 X 却声称 X」= bug,自主修,不用问。已修:`score === undefined` → 返回 null → 不落库。 | 已决策 |
| 019 | 2026-07-16 | 产品方 | 红线与指标(设计原则) | 其他 | 【红线设计三原则】2026-07-16 实测得出,已写入 LOOP.md。(1) **精确率天然可用"少预测"刷分,必须配召回率**;(2) **分母必须用金标定死的量,不能用 AI 能控制的量**;(3) **一条红线只防一个方向,成对的红线才防得住取舍**——设计新红线时先问:想刷这条线最省事的做法是什么?那个做法会伤到什么?把那个"什么"也变成红线。 | 实测证据:(1) 首屏精确率 40.9%→52.9% 看似涨 12 点,逐条核对是 9/22→9/17——**分子是同样那 9 条,连集合都一模一样**,掉出分母的 6 条无一是金标高;摁回分母 22 = 40.9%,零改善。召回率分母由金标定死(金标高总数),AI 控制不了。(2) 首屏可用率分母误用"导出全部故事",导出从 40 变 100(run-ranking.ts 改动)→ 12.5% 被算成 5.0% 假 FAIL;按金标对齐的口径天然抗漂移(召回精度纹丝不动),按产物规模算的不抗(见 012)。 | 产品方 2026-07-16 拍板,已写入 LOOP.md。 | 已决策 |
| 020 | 2026-07-16 | 产品方 | LOOP 机制(停机规则) | 其他 | 【停机规则收紧】2026-07-16 一轮 loop 停了 7 次问人,**只有 1 次是真产品决策**,其余 6 次把"修 bug"和"内部实现选择"误当决策。收紧后三条必停:(1) 用户能看到的东西变了;(2) 要改金标/标准/指标定义/红线数值;(3) 红线之间真冲突(改动让某条红线 FAIL 且无法两全)。新增分界线:**「我们不知道 X 却声称 X」= bug,自主修;「我们知道 X,但要不要告诉用户/怎么告诉」= 决策,问人**。 | 产品方判定:规则太宽 = 看起来谨慎,实则失职——**每次不必要的停机都在把判断责任推回给人,而人雇这套 loop 就是为了不用做这些判断**。特别澄清:**@pipeline-auditor 判"管道有问题"后,停机的目的是让结论作废,不是让每个修法都问人**——修管道本身照常自主推进。 | 产品方 2026-07-16 拍板,规则已收紧。 | 已决策 |
| 021 | 2026-07-16 | 主会话 | 重排打分 / 前端渲染 | 跨环节风险 | 【残留 null 不需要全挂就会发生】主会话核实:在**完全正常、未降级**的故事里也存在单条未打分候选,它们照样吃 page.tsx 的 `?? 100` → 被标成「高匹配」进首屏。实例:S051(14 道里 1 道未打分 →「你休息时会小睡一会儿吗?」)、S056(9 道里 1 道 →「你喜欢拍风景照吗?」)、S057(9 道里 1 道 → 同上,与 S056 同一道题)。 | S056/S057 漏的是同一道题,且它与「你喜欢拍不同风景的照片吗?」几乎同义——**模型在近义题簇上丢题,与 014 的 UUID 错位是同一指纹**。意义:原设想的降级方案(检测"全挂"→重试→落中性展示)**够不着这些故事**,因为它们不是降级态。另查明(读代码,非推测):ranking.ts 的 `checkAlignment` 已含漏题校验(`for (const id of seqMap.keys()) if (!covered.has(id)) problems.push('漏题')`),故**半挂会触发重试**;但重试仍半挂时,`fallback` 走 `keepAligned` **返回部分结果**,剩余条目保持 null → 仍吃 `?? 100`。 | 未处理。待产品方决定:正常故事里混进的未打分题该如何呈现(不展示 / 折叠区最底 / 标「未评分」)。关联 016(展示层 `?? 100`,仍在产品方手上)、018(落库层已修)。 | 待议 |
| 022 | 2026-07-16 | 产品方 | 重排打分(AI 边界 / 错误处理) | 其他 | 【第6个同病 bug —— `catch → return []` = 在边界上消灭异常】根因(已查证):src/lib/llm.ts:113 的 `const timeoutMs = opts.timeoutMs ?? 30_000` 是全局默认,ranking.ts 未传 → 吃 30 秒;候选多的故事生成时间逼近 30 秒 → AbortController abort → `rawCall` 抛错(它只有 `finally { clearTimeout }`,无 catch)→ 抛穿 `callLLMJson` → 被 `rankQuestions` 最外层 `catch { return [] }` 吞掉 → 全部候选 null → 再经 page.tsx 的 `?? 100` → **全部标成「高匹配」**。证据:14-42 全量跑的 6 个全挂故事,候选数 35/35/35/26/24/8,**6 个里 5 个挤在 100 故事的最高位**(中位数仅 7);S077 同为 35 道却擦边过 → **同样大小纯看运气**;S005(8 道)是另一种病(瞬时抖动)。「全空而非部分」是铁证:abort 发生在 fetch 层,一个字节都没拿到,专门的截断兜底 `recoverPartialScores` 无从抢救。 | 与前五个同病 bug 的区别:前五个是「不校验 / 静默兜底」,**这一个是主动销毁证据**——异常本已产生、已沿调用栈往上跑,是我们在边界上把它掐死并伪装成"正常的空结果"。 | 产品方 2026-07-16 拍板。铁律:**AI 边界的 catch 不许吞——要么重抛,要么记录并发信号。`return []` 不是错误处理,是错误销毁。** 已批准修法:`timeoutMs = 15_000 + 1500 × 候选数`(实测标定:7 道≈11s、35 道≈20s,新超时给 2.3×~3.4× 余量)+ 同样大小的重试一层治瞬时抖动(**不用指数退避——超时的请求重试还是同样大小,退避治不了超时,它治的是抖动**)+ `rawCall` 加 catch 记录失败事实后重抛。配套决策:**「原始输出留存」与「失败事实记录」必须分开**——前者含用户故事、有隐私与存储成本,属调试功能,opt-in 默认关;后者**必须永远记录**,与 `LLM_RAW_LOG_DIR` 无关,至少 console.error + 结构化字段(service / 输入规模 / 耗时 / 错误类型),为接监控留口子。关联 017(AI 边界数据契约)、016/021(`?? 100`)。 | 已决策 |
| 023 | 2026-07-16 | 产品方 / 主会话 | 重排打分(诊断方法) | 其他 | 【maxTokens 假设被实测推翻】产品方提出假设:6 个全挂是 maxTokens 截断(S030 有 24 道候选 × 每道≈40token ≈ 960token,可能超)。主会话初次估算支持该假设,甚至警告「新格式的 q 回显让 35 道输出达 4130 token,已顶穿 4096」。**实测推翻**:用留存的原始输出量得每条候选实际只占 **104 字符**(≈42-52 token),35 道 ≈ 1500-1800 token,`maxTokens: 4096` 有**一倍余量**;主会话的估算**高了 2-3 倍**。 | 与 010/011 同源:又一次"在错的地方找原因"。区别是这次**在动手前被实测拦住了**——拦住它的正是当天上午才加的原始输出留存机制。 | 产品方 2026-07-16 拍板。教训:**拍脑袋估算高了 2-3 倍,差点让我们去修一个不存在的病。更糟的是——如果照那个假设改了 maxTokens,真凶(30 秒超时,见 022)会继续存在,而我们会以为修好了。** | 已决策 |
| 024 | 2026-07-16 | 产品方 / 主会话 | 重排打分(调用结构) | 其他 | 【「横向比较」不可分割:分批与单独补题都被否】产品方与主会话独立得出同一结论,两个方案因同一理由被否:**方案B「候选超过 N 道时分批调用(每批 8 道)」**——否;**「补缺循环只单独问缺失的那几道」**(产品方原提出,后自行纠正)——否。 | 共同理由:重排的本质是**横向比较**——模型要在 35 道里判断哪道最贴合。分批后每批只看得到 8 道,**第 1 批的"最好"和第 4 批的"最好"用的不是同一把尺**,这会引入一种**我们没有指标能测到的偏差**。单独问 q7 同理:它的分数不再是"在 35 道里横向比较"的结果,而是孤立判断。(方案B 的 token 成本 input 翻 5 倍只是次要理由,主要理由是尺子不统一。) | 产品方 2026-07-16 拍板。已采纳替代:补缺循环保持**整批重问 + REALIGN_INSTRUCTION**,只把"重试一次"改成"最多循环 3 轮",**尺子完全不变**。已有证据它 work:复现跑里 S022/S030 各漏 1 道(q4、q10),整批重问一次即补齐。3 轮后仍缺 → 不展示 + 必须打日志。关联 021(半挂残留 null)。 | 已决策 |
| 025 | 2026-07-16 | 产品方 | 评测口径 / 影响面估算 | 跨环节风险 | 【6/100 全挂率是评估脚本口径,非生产真实值】产品方指出:6/100 这个失败率来自 scripts/eval/run-ranking.ts:73 的 `CONCURRENCY = 3` —— **评估脚本让 3 个请求并发、互相竞争 API,把候选多的故事推过 30 秒**。**生产是单用户单请求,没有这种自我竞争**,真实全挂率应显著低于 6%。 | 意义:修超时(见 022)仍然必要(生产也有慢请求、也有网络抖动),但**不要用 6/100 去估算线上影响面**。台账里凡引用该数字处应加此限定——现存引用点:016(「6/100 的用户会看到全部候选题都标着「高匹配」」)、022(6 个全挂故事的候选数分布)。 | 未处理。产品方 2026-07-16 提出并明确标注待议:该限定是否需回溯写入 016 等既有条目、以及生产真实失败率如何测量,待人拍板。记录员按台账规则**未改动 016 的既有表述**。 | 待议 |
| 026 | 2026-07-16 | 工程(fix-engineer) / 产品方 | 重排打分(管道) | 合并记录 | 【重排管道错位修复】**改了什么**:`src/services/ranking.ts` join key 由 36 位 UUID 改为短序号 q1/q2(`seqOf`/`seqMap` 只活在 `rankQuestions` 内,**模型永不见 UUID**);新增题干前缀回显字段 `q` + `checkAlignment` 校验 + `echoMatches`;`isRankingResponse` 加 `q`;`recoverPartialScores` 加 `keepAligned`(抢救≠放行)。`src/services/matching.ts:171-197` 加 id 集合校验(未知 id → console.error 后丢弃;重复 id → 保留首次,不再静默"后者胜";缺失 id → warn 并列出 missingIds)。`src/lib/db/questions.ts:107-111` 加 `.order('question_id')`。 | **为什么**:依据台账 014(管道审计判定"管道有问题")——模型把 {score,reason} 整体贴到错误 id 上,而 score 与 reason 读自同一对象、**原子同源**(`matching.ts:175-176`),理由错到哪分数就错到哪。 | **影响用户可见行为:有**。修复前用户可能看到"分数打给 A 题却挂在 B 题上"的匹配结果;修复后错位条目**宁可不给分也不贴错题**。**关键设计**:单纯的 id 集合校验**抓不到互换**(q1/q2 整体对调时集合仍合法),只有题干回显能抓到——该设计由产品方提出(见 014 临时处理列)。 | 待议(代码已合并,待人确认结案) |
| 027 | 2026-07-16 | 工程(fix-engineer) | 萃取(extraction) | 合并记录 | 【extraction taxonomy 白名单校验】**改了什么**:`src/services/extraction.ts:189-254` 新增 `TAXONOMY_CODES`(49 个,**从 SYSTEM_PROMPT 现场解析**,不另抄一份)+ `isValidPick` + `isExtractionResult`(primary 与 secondary **都**校验)+ `EXTRACTION_FIX_INSTRUCTION`;`:275-276` validate 由原校验换成 `isExtractionResult`。`src/services/matching.ts:65-88` 加二道网:`meta` 缺失 → console.error + 返回 null,删掉 `?? pick.pointCode` 与 `: '情绪内核'`。 | **为什么**:依据台账 017 的已决策铁律(跨 AI 边界的数据必须校验、必须留证、不许 `??` 兜底)。原 validate 只校验 pointCode 是 string → 模型编造 `EMO_99` → 静默把 dimension 兜底成「情绪内核」→ **凭空捏造的维度显示给用户** → 召回 0 走 noMatch,**日志一个字都没有**。 | **影响用户可见行为:有**。修复前非法 code 会让用户看到**捏造的维度名**;修复后如实走 noMatch。**验收**:萃取基线 **281/297 = 94.6%,分子分母与改动前完全一致**;三方比对 prompt=49 / DB=49 / ADJACENCY=49,零漂移。 | 待议(代码已合并,待人确认结案) |
| 028 | 2026-07-16 | 工程(fix-engineer) / 产品方 | 落库(持久化) | 合并记录 | 【落库层 `?? 100` 修复】**改了什么**:`src/app/api/matching/route.ts:16-23` 的 `levelForScore` 由 `const s = score ?? 100` 改为 `if (score === undefined) return null`。 | **为什么**:依据台账 018 的产品方拍板(2026-07-16,定性为「我们不知道 X 却声称 X」= bug,自主修不用问)。原实现在重排降级或模型漏题时,把全部候选以 `match_level='high'` **永久写进 `corpus_question_matches`**,下游任何读这张表的功能都继承这个谎。 | **影响用户可见行为:间接有**。不再往库里写虚假的 high 档,下游功能不再继承。与展示层 `?? 100`(016/021)区分:**展示层是瞬时的、是文档写明的产品决策,仍在产品方手上;落库层是持久化的,无辩护理由**。 | 待议(代码已合并,待人确认结案) |
| 029 | 2026-07-16 | 工程(fix-engineer) / 产品方 | 重排打分(AI 边界 / 错误处理) | 合并记录 | 【超时预算 + 补缺循环 + 失败留证】**改了什么**:`src/services/ranking.ts:21-30` 新增 `TIMEOUT_BASE_MS=15_000` / `TIMEOUT_PER_CANDIDATE_MS=1_500`,`:384` `timeoutMs = 15_000 + 1_500 × 候选数`;`:32-37` `MAX_ATTEMPTS = 3`,`:399-416` 3 轮用尽仍缺 → console.error **点名哪几道**。`src/lib/llm.ts:59-65` 新增 `maxAttempts?`(默认 2,**其余调用方行为不变**);`:249-297` 由"重试一次"改为"最多 N 轮"循环,每轮把模型上轮输出 + 整改要求追加进会话,**完整原始输入始终在场**;`:262-277` `rawCall` 加 catch → 记录失败事实 → **原样重抛**(不吞、不改控制流);新增 `LLMHttpError` + `classifyFailure`。 | **为什么**:依据台账 022 的产品方拍板(铁律:AI 边界的 catch 不许吞;`return []` 不是错误处理,是错误销毁)+ 024 的产品方拍板(补缺循环必须**整批重问**,尺子不变)。根因:`llm.ts:113` 的 30 秒全局默认超时 → abort → 抛穿 → 被 `rankQuestions` 的 `catch { return [] }` 吞掉 → 全部 null。 | **影响用户可见行为:有**。修复前候选多的故事会**整体降级**(全部候选无分数);修复后 6 个全挂故事全部恢复。**验收**:7 故事 198 候选,未打分 0,零调用失败;S048/S049/S078(35 道)新超时 67.5s vs 旧 30s;补缺循环实证 7 次首发 → 4 次进第 2 轮 → 1 次进第 3 轮 → 全部 ok;测试锁死"第 3 轮请求里 35 道候选原文一字未少"(**尺子没变**)。**注意(行为变更)**:`fallback` 现在收到**最后一轮**的 raw(原为首轮)——3 轮重问下拿首轮抢救等于扔掉中间两轮修正成果;唯一另一消费者 `restructure.ts:52` 对 raw 来源不敏感。 | 待议(代码已合并,待人确认结案) |
| 030 | 2026-07-16 | 工程(fix-engineer) / 产品方 | 可观测性(原始输出留存) | 合并记录 | 【原始输出留存机制】**改了什么**:`src/lib/env-server.ts:31-35` 新增 `llmRawLogDir`(读 `LLM_RAW_LOG_DIR`,留空 = 关闭);`src/lib/llm.ts:76-104` 新增 `appendRawLog()` 按日期落 JSONL,`:207-235` 首发与重试**成功也留存**;`.gitignore:38-39` 加 `/.llm-raw/`(含故事原文,**永不入库**)。 | **为什么**:依据台账 014(错位一直没被发现的原因之一:`llm.ts` 成功调用不留存原始输出 → 错位不可见也不可复盘)+ 022 的配套决策(「原始输出留存」与「失败事实记录」必须**分开**:前者含用户故事、有隐私与存储成本,属调试功能 opt-in 默认关;后者必须永远记录)。 | **影响用户可见行为:无**(默认关闭,opt-in 调试功能)。**第一次兑现**:同日 `--runs=1` 得 93.9% 看似掉 1pp,原始输出显示 300 次调用**重试 0 次、越界 code 0 个** → 代码路径与旧实现逐条等价 → **30 秒内排除"误杀"嫌疑**,再用同口径 R=3 复现 281/297。上午加的机制下午就救了一次误判(另见 023:同一机制在动手前拦下了 maxTokens 假设)。 | 待议(代码已合并,待人确认结案) |
| 031 | 2026-07-16 | 产品方 | LOOP 机制 / 产品制品 | 其他 | 【建立产品不变式制品 `docs/PRODUCT_INVARIANTS.md`】产品方 2026-07-16 指出三个洞:(1) 台账只记"报上来的"不记"做了的";(2) 红线只覆盖 ranking + 萃取,**analysis/practice/restructure 零红线**;(3) **产品定位不在任何制品里,停机规则依赖 agent 自己判断"这算不算产品决策"**。新建 `docs/PRODUCT_INVARIANTS.md`,9 条规范性陈述句,逐条带 `[来源:拍板日期/台账编号]`。 | **与权威文档的分工**:权威文档是**描述性**(现在怎么工作),不变式是**规范性**(什么不许变)。描述变了改前者,承诺变了才改后者。**第 1 条与第 9 条是一对对冲**:第 1 条防"骗"(给答不了的题还标高匹配),第 9 条防"空"(把首屏优化成诚实的荒地)。判断句:**只是把"骗"换成"空" = 在挪问题,必须上报。** | 配套已落地:加入 `guard-golden.sh` 保护清单(**只有人能改**);`pm-reviewer.md` 职责二改为"第一步硬性逐条对照 9 条,任一条动摇 → 挂旗停机,且**必须逐条给结论、不许只报有问题的**"。本条(洞 1)即「合并记录」类型的由来。 | 已决策 |
| 032 | 2026-07-16 | 工程 / 主会话 | 回归守卫(保护清单) | 其他 | 【guard-golden 漏保护算分脚本(已补)】`scripts/eval/run-ranking-score.ts` 此前不在保护清单里。**那是算分脚本,四闸门的口径全在其中——它就是指标定义**,与金标同级。fix-engineer 此前本可随手改它而不被拦。 | 认知:**我们保护了金标,却漏了"定义金标怎么用"的东西。** 关联 019(红线设计三原则:分母必须用金标定死的量)、012(分母口径 bug)。 | 已补入 PROTECTED 清单,实测拦截生效(exit=2)。**记录员注**:转来的条目标为「已决策」,但未附人/产品方的拍板原话或拍板日期,故按台账规则记「待议(代码已补,待人确认结案)」,沿用条目 009 先例。 | 待议(代码已补,待人确认结案) |
| 033 | 2026-07-16 | 主会话 / 工程 | 召回(题目映射) | 跨环节风险 | 【invariant #3 只靠数据约定,代码不保证】`docs/PRODUCT_INVARIANTS.md` 第 4 条「topic_only 题只经首页泛题池,不走召回」**不由代码保证**:`src/lib/db/questions.ts:99-113` 的 `getQuestionsByObservation` 查 `question_observation_links` 表,**没有任何 `topic_only` 过滤**。只要有人给一道 topic_only 题加了 link 行,它立刻会被召回,**没有任何东西会拦**。 | 主会话原话:**一条只靠约定维持的不变式,等于没有不变式——只是还没人碰它而已。** | 未处理。转来的建议:加校验或测试把这条约定钉死;在那之前,改动召回逻辑或题目映射时必须人工确认。待人拍板。 | 待议 |
| 034 | 2026-07-16 | 工程(fix-engineer) / 主会话 | analysis / phrases / polish / restructure(超时) | 跨环节风险 | 【analysis 是下一个超时受害者】fix-engineer 实测(worst case:Part 2 + 带故事):Analysis maxTokens=2560 / 11.8s / 30s 余量 **2.5×**;Phrases 2048 / 10.1s / 3.0×;Polish 未设 / 2.4s / 12.3×;Restructure 未设 / 0.8s / 37.6×;Extraction 1024 / ~2.2s / ~13×。**全部 4 个服务都没传 `timeoutMs`,全吃 30s 默认值。** | analysis 比 ranking 更危险的四个理由:(1) **参照系**:ranking 35 道实测 20s(1.5× 余量)就是全挂的那条线,analysis 离它只差一步;(2) `maxTokens: 2560` 允许输出再翻约 2 倍 → 外推 ~24s,余量基本归零,**而实测用的故事只有约 130 字符,真实用户语料长得多**;(3) **后果更硬**:analysis 没有 fallback、没有截断抢救,超时 = 直接抛错 = **用户看到报错页**(ranking 至少能降级展示);(4) 它是**交互式**的,用户在前台等,且 analysis 页同时触发 `[Analysis]` + `[Phrases]` 两个调用。关联 022(同一根因:30s 全局默认超时)。 | 已派 fix-engineer 补显式 timeout(进行中)。**遗留待议**:`llm.ts` 的 30s 默认值本身——它对短输出合理,对长输出是陷阱;fix-engineer 建议**取消默认值、强制每个调用方显式声明预算**(会动全部 5 个调用点,**超出当轮范围**),待人拍板。**注意**:analysis/practice/restructure 是**零红线环节**(无 eval),已按 LOOP.md 新增的必停第 4 条挂旗(关联 031 洞 2)。 | 待议 |
| 035 | 2026-07-16 | 主会话(Prompt 1 只读调查) | 重排打分(判分档口径) | 指标争议 | 【prompt 写「30-59=低」,constants.ts 写 SCORE_LOW=40,30-39 是三方口径分歧带】`src/services/ranking.ts` 的 SYSTEM_PROMPT 判分档原文:「30-59(低):必须换一个不同的经历或故事才能答,当前故事帮不上,即使话题沾边也在这档。」「0-29:完全答非所问。」而 `src/lib/constants.ts:29` 是 `export const SCORE_LOW = 40`,注释「< SCORE_LOW:隐藏(软折叠,非硬删)」。`ENGINEERING.md:180` 写「必须换故事 30–59(低);<30 隐藏」——**与 prompt 一致,与 constants.ts 不一致**。后果:30-39 分这一带,**模型认为自己在打「低匹配」**(prompt 明文告诉它 30-59 是低档),而代码全部按「隐藏」处理:`page.tsx`(不展示、不计入 totalVisible)、`route.ts`(不入库)、`run-ranking-score.ts` 的 `aiTierOf`(判「隐藏」)、`run-ranking.ts` 的 `bucketOf`(判 hidden)。 | **四处判档逻辑本身完全一致**(都是 `>=` 阶梯,无 `<=`),40 分整显示、39 分隐藏——**代码内部无矛盾,矛盾在「代码 vs 喂给模型的标准」之间**。即我们用一把尺告诉模型怎么打分,再用另一把尺处置它打出的分。 | 未处理。主会话已明确不在本轮修(本轮改动只许有一个变量),留给三级标签那次一起解决。 | 待议 |
| 036 | 2026-07-16 | 主会话(Prompt 1 只读调查) | 算分(隐藏区加权) | 指标争议 | 【scale 权重每轮重算,且与金标声明的抽样率对不上】`scripts/eval/run-ranking-score.ts:288`:`const scale = n > 0 ? hiddenTotal / n : null`。字段定义(`:237-240`):`hiddenLabeled` = 隐藏区已标总数 n;`hiddenTotal` = 隐藏区候选总数 N_hidden(**来自导出**)。实测三个对不上的数:金标声明 `hiddenSampleRate: 0.2` → 理论 scale = 1/0.2 = **5.0**;旧基线算出 `204/59` = **3.457**;新基线算出 `216/59` = **3.661**。**分母 n 恒为 59(金标里 hidden_sampled 的条数),分子 hiddenTotal 随每轮导出的分数变**(204→216)→ scale 每跑一次变一次,且都不等于 5.0。`run-ranking-score.ts:45` 注释写明这是有意设计:「hiddenSampleRate?: number // 隐藏区抽样率(如 0.2);算分优先用数据实测 S,此值仅交叉校验/留档」。 | 入选概率是**历史事实**——标注当时按什么规则抽的,权重就该是那个规则定死的常数。用本轮实测反推权重,等于**让被测对象决定自己的称重方式**:管道越准,候选跨区越多,hiddenTotal 越变,权重跟着变。**关联**:与「金标 zone 冻结」同根(zone 按标注时的导出分数冻结,新分数让候选跨区)。已知实例:S016「弄坏东西的一次经历」标注时 <40 进隐藏区,新代码下 88 分已是可见区 AI高,但混淆矩阵仍按冻结 zone 加权 ×3.457 → 高行·低 = 3(可见)+1×3.457 ≈ 6.66,而虚高榜只数 4。关联 019(红线设计原则 2:分母必须用金标定死的量,不能用 AI 能控制的量)。 | 未处理。属指标定义 = 必停第 2 条。 | 待议 |
| 037 | 2026-07-16 | 主会话(Prompt 1 只读调查) | 红线与指标 | 指标争议 | 【红线 4「首屏精确率」没有配对的召回率,违反 LOOP.md 自己的原则 1】`scripts/eval/loop/LOOP.md:126` 原则 1 原文:「对策:任何精确率都要配一条召回率。召回率 = 判对的 / 金标里本来有的,分母由金标定死。」查证:`BASELINE.json` 里**不存在**分母=金标高总数(11) 的召回率,全部 key 已逐一核对(`_说明`/`_噪声容差`/`noise_margin_pct`/`_萃取`/`extraction_strict_pct`/`_重排_导出层`/`ranking_nomatch_max`/`ranking_visible_zero_max`/`_重排_质量层`/`ranking_firstscreen_precision_pct`/`ranking_inflation_rate_pct_max`/`ranking_firstscreen_usable_pct`/`_召回_映射层`/`ranking_recall_precision_pct`/`_召回精度口径`/`_召回精度盲区`/`_对冲说明`/`_首屏可用率口径`),`regression-all.sh` 里也搜不到。⚠️ **区分两个同名不同物**:`ranking_recall_precision_pct`(**存在**)= 召回**精度**,分母 = 召回∩金标已标的全部对数(148),测的是**映射准不准**;LOOP.md:126 要求的召回**率**(**不存在**)分母 = 金标高总数(11),测的是**有没有漏掉真的高**。 | 原则 1 的实证正是首屏精确率——2026-07-16 实测 40.9%→52.9% 看似涨 12 点,逐条核对是 9/22→9/17,**分子一个没动,命中的就是那同样 9 条**。**原则写下来了,配对的召回率却没装。** 补充实证(同日 R=3):**分母由金标定死的指标三轮完全一致**(首屏可用率 12.5/12.5/12.5、召回精度 30.4/30.4/30.4、精确率分子 9/9/9);**分母由 AI 控制的指标抖 3~5pp**(首屏精确率 39.1/39.1/36.0,AI高分母 23/23/25)。关联 019(已决策的红线三原则)、008(召回精度盲区)。 | 未处理。新增红线 = 必停第 2 条。 | 待议 |
| 038 | 2026-07-16 | 产品方 / 主会话 | LOOP 机制(agent 职责边界) | 合并记录 | 【agent 职责扩范围:补上「金标本身对不对」这个分工盲区】**改了什么**:(1) `.claude/agents/pipeline-auditor.md`:description 加「包括金标这一端」;正文新增「你的范围包含金标」一节;必查清单 5 项 → **7 项**(新增 ⑥ 金标结构自洽性、⑦ 抽样设计校验);总判改为「金标端的问题同样触发『管道有问题』」;禁止项新增「评价金标某一条该标什么档」。(2) `.claude/agents/diagnostician.md`:【金标可能错】改为**【疑似金标错误】**并新增专节,写死两个证据门槛(R≥3 全轮稳定分歧 + 理由必须引用 rubric 原文指出金标违反自己的明文定义);写入铁律原文;新增「分诊标出的条数本身是金标质量的代理指标」(148 条标 5 条→尺子可信;标 40 条→尺子不能用);禁止项新增三条。(3) `scripts/eval/loop/LOOP.md`:新增「金标复审队列」触发规则——**AI 稳定分歧(R≥3 全一致)+ 理由引用 rubric 原文且自洽 → 自动进金标复审队列,不许自己改、不许绕过**。(4)**【2026-07-16 主会话补正,原清单漏报此项】**`pipeline-auditor.md` 禁止项:原「改任何文件。」→ 改为「改任何文件。**注意:你没有 Write/Edit 工具,但你有 Bash——用 Bash 写文件同样是违规**(这条目前靠自觉,没有 hook 拦你)。」;`diagnostician.md` 禁止项:原「写代码、改任何文件。」→ 改为「写代码、改任何文件。**注意:你没有 Write/Edit 工具,但你有 Bash——用 Bash 写文件同样是违规**(这条目前靠自觉,没有 hook 拦你)。」**该项关联 039**——它是 039 那个洞(hook 挂在 agent 上、matcher 只认 `Write\|Edit`,Bash 可绕过)的**临时缓解**,且明文承认自己只是自觉、不是护栏。 | **为什么**:产品方 2026-07-16 指出——七个 agent 里 `@pipeline-auditor` 审「喂给模型的输入对不对」、`@diagnostician` 做「AI 错例归因」,**没有任何一个 agent 的职责包含「金标本身对不对」**。证据:今天三个最值钱的发现里,管道错位有人负责找,而**金标存疑(S074)与金标不自洽(台账 004)全是撞出来的**。LOOP.md 的忠告写着「尺子 → 数据 → 模型」,规则却漏了第一环。背景事实:`scripts/eval/golden/ranking.v1.json` 元数据全文仅 `annotator: sianchenn` / `annotatedAt: 2026-07-16` —— **单人、单遍、无 source、无一致性数据、无轮次、无修订记录,labels[] 无第二标注人无分歧记录**。对照 `extraction.v2.json`:`双人独立标注,主观察点一致率99/100,三项全一致96/100,分歧经讨论定稿` + source + revisedAt + revisionNote。**重排金标是单人单遍、零一致性数据,却被用来守 ±1.5pp 的红线。** | **影响用户可见行为:无**(仅 agent 岗位说明书与 loop 规则,不涉任何运行时代码)。**核心约束(已写进岗位说明书原文)**:**AI 只做分诊,不做判定。** AI 有把握的时候恰好是它与金标强烈分歧的时候,也恰好是循环性最致命的时候——用模型的意见覆盖金标、再拿改过的金标去测这个模型,测出来的是它跟自己的一致率。**只出清单,人做全部判定。改金标 = 必停第 2 条。** 关联 004(金标不自洽)、015(S074)、039(Bash 绕过 hook)。**【漏报事实,主会话 2026-07-16 自认并要求如实记录】**:本条目首次转来时,「改了什么」只列了 3 项,**漏了第 4 项(两份禁止项的 Bash 提醒)**。记录员核对 039 的临时处理列时发现不对齐并顶回,主会话确认原话:「在我眼里那是『顺手补的一句提醒』,不是『一次改动』——**而这正是新规则(每次合并一条)当初要防的那种心态**。」对照台账规则变更原文:此前 `rawCall`/`checkAlignment`/`validate` 全没进台账,因为「在执行者眼里它们是实现细节」。**新规立起来的第一轮,就抓到了立规则的人自己。** 主会话判定:这个事实比那两行禁止项本身更有价值。 | 待议(改动已合并,待人确认结案) |
| 039 | 2026-07-16 | 主会话(顺带查出) / 产品方 | LOOP 机制(hook 护栏) | 跨环节风险 | 【guard-golden 的保护有个 Bash 大小的门】实查结果:`diagnostician` hooks=0 / tools=Read,Grep,Glob,**Bash**;`pipeline-auditor` hooks=0 / 同上;`pm-reviewer` hooks=0 / 同上;`red-team` hooks=0 / 同上;`metric-designer` hooks=0 / tools=Read,Grep,Glob(**唯一物理只读**);`fix-engineer` hooks=2 / tools=Read,Grep,Glob,Edit,Write,Bash;`recorder` hooks=2 / tools=Read,Edit,Write。**hook 挂在 agent 上,不是文件上**,且 fix-engineer 的 matcher 原文是 `matcher: "Write|Edit"`。两个洞:(1) pipeline-auditor / diagnostician / pm-reviewer / red-team 的「只读」**是纯约定,无强制**——它们没有 Write/Edit(这层是真的),但**有 Bash**,`echo > golden.json` 之类可以写;岗位说明书里的「Bash 仅限只读」是祈使句,不是护栏。(2) **连 fix-engineer 的 guard-golden 也能被 Bash 绕过**——matcher 只匹配 `Write|Edit`,走 Bash 写文件不触发 hook。 | loop 的核心设计宣称「改代码的人碰不到考卷,hook 直接物理拒绝——不是靠它自觉」(`LOOP.md:21`)。**实际上该保护对 Bash 路径失效。** 关联 032(guard-golden 曾漏保护算分脚本)、038(改金标 = 必停第 2 条,该禁令目前同样只靠自觉)。 | 本次已在 `pipeline-auditor.md` / `diagnostician.md` 的禁止项里补了明文提醒(「用 Bash 写文件同样是违规,这条目前靠自觉,没有 hook 拦你」),但**那仍是自觉,不是护栏**。产品方要求本次只报不修。待人拍板。 | 待议 |
