# 交接 · 2026-07-17 深夜 → 内测转向

> **给下一个窗口的 Claude Code。先读我，再按需读 HANDOFF.md / BASELINE.json / 内测评估方案.md。**
> **本文补齐了 HANDOFF.md 和台账里没记的「今晚后半段」全部内容。**

---

## 0. 你的第一件事（两条，别跳过）

### ① 通道 bug —— 已定位、已解决，但你要知道
上一个窗口反复遇到「终端输出被文本损坏」：命令输出里随机混入无关词（Warcraft/railway/2222/3333/伪解释文本），连读文件、Read 工具、git log 都被污染，甚至伪造假的 system reminder。
**根因已确认：是【钩子（hooks）】在每条命令后注入输出，损坏了通道。产品方关掉钩子后，短/中/长输出全部恢复干净**（jest 113 passed 完整穿过、9 行 agent model 完整读出）。
- **如果你又见到输出混乱**：先怀疑钩子。让产品方确认钩子是关的。
- **受损时的工作纪律（万一复发）**：命令保持短输出、`> /tmp/x.txt 2>&1` 落盘再 `cat`、只信明确标记（MARK-A/MARK-B）之间的内容、不可逆操作（commit/改金标）前多次交叉验证。
- 尾部偶有一个无害的 ` ``` ` 附着，忽略它。

### ② 当前 git 状态：⚠️ 更正 —— 今晚工作【曾未提交】，本窗口已补做封存
> **2026-07-17 次窗口更正**：下面这段「已封存」是**假的**。核对 `git log`/`git reflog` 证明：所列 5 个 commit **从未存在**，HEAD 只到过 `edc2cd2`。根因即 §0① 的通道 bug——上个窗口在受损通道里收到伪造的「提交成功」，信以为真写进了本文。**工作没丢，全在工作区**，本窗口经产品方拍板后按下述 5 段分组【重新真正提交】。以下原文保留作病历。

上个窗口以为把今晚的工作分 5 个 commit 提交了（**先 `git log --oneline -8` 核对**）：
```
d9b98be chore(eval): 萃取金标 xlsx 入库 + gitignore 忽略 .bak
ede89e4 docs(内测): 内测评估方案 + 隐私草案入库   （message 有通道注入的错字"文档"，可 rebase 改）
d885224 fix(privacy): 留证目录权限收紧 + LLM_DEBUG 生产禁用
d1a7f47 feat(eval/loop): BASELINE v3 + digest 防篡改 + 双角色
9724bf3 docs(golden): 金标 7 条表外补裁（中→低）
edc2cd2 （今晚开工前的基线）
```
**工作区现在只剩 `M .claude/settings.json`（产品方关钩子的改动，别碰）。** 分支 feat/ranking-three-tier，未 push。

---

## 1. 一句话现状

**尺子（合成评测集 + v3 红线）已冻结在「够用」状态，战略重心转向内测。代码/金标零待办，卡点全在「产品方要拍的几件事」和「内测前置三件硬活（留证/埋点/隐私）」。**

---

## 2. 战略拍板（产品方 2026-07-17 逐字原话，这条定一切优先级）

> 「那我们现在把该做的都做了，比如金标提交，然后合成红线冻结在够用状态，把大头转移到内测」

**含义（上个窗口和产品方一起厘清的）**：
- 40 个合成故事的评测集是**「校准仪器的尺子」，不是「度量产品的尺子」**。它的活基本干完了：管道修好、系统性病根找到、造尺子的工艺练成。
- **停止精雕合成红线**。设计官还有 9 条精细红线提案 → 全部「**记录在案，内测建金标 v2 后重钉**」，不必现在逐条精拍。够用即止（能挡管道塌方 + 发现系统性问题，现在已够）。
- **首屏虚高的手术缓办**。诊断官定性：首屏虚高去重仅 5 条、AI 真判错的独立题仅 2 条、且是合成数据。病根「AI 把话题字面相关当可答」诊断保留进内测，但「现在在合成数据上改 prompt 修它」性价比差（改 prompt 是全局手术、会漂 13 条、且改完在即将被取代的尺子上验证）。
- **大头转内测前置**：留证 / 埋点 / 隐私。

**为什么该走内测（理性依据）**：合成数据天花板已到——首屏可用率 15% 封顶是**题库问题**（AI 已满分 6/6），继续在合成数据上打磨学不到新东西。真实题材分布、真实供给缺口疼不疼，**只有内测能答**。

---

## 3. 内测前置清单 · 现状体检（决定内测能不能开）

| 前置项 | 状态 | 卡在哪 |
|---|---|---|
| ① 全链路留证 | 🔴 **未做** | ASR（`src/services/transcription.ts`）不经 llm.ts，无留证，链路第一环盲区。**且它是最大隐私敞口**（补它=全量语音转写落盘） |
| ② 002 渲染缺陷 | ✅ 已修（`dd418d5`） | — |
| ③ 文档对齐三档语义 | 🟡 部分 | PM 说这是唯一「砍了只疼内部」的一项 |
| ④ 隐私 | 🟡 草案已写未定 | `docs/内测隐私方案-草案.md` 在，**四问待产品方拍**（见 §4） |
| ⑤ 埋点 | 🔴 未做 | 方案已有（见 §5），零代码。L1 每个指标都靠它 |

**能开测的关键路径**：④隐私四问（产品方拍，不写代码，最快）→ ①⑤留证+埋点（写代码，量最大）。**④决定①怎么写**（脱敏在哪一环直接改留证代码形状），所以先④。

---

## 4. 隐私四问（待产品方拍，上个窗口已用大白话摆过，产品方还没答）

**背景事实（已查证，影响第 1 问）**：故事文本→**阿里云通义千问**（qwen），语音→**字节跳动 openspeech**。两家**标准 API 条款都声明不用于训练模型**（除非换 Coding Plan 类特殊套餐——届时脱敏从可选升必须）。所以「数据被拿去训练」目前不成立，真正要管的是**我们自己怎么存**。

**问题 1 · 脱敏在哪一步？**（有坑）
- A 入库即脱敏：改产品行为（AI 吃脱敏文本，匹配结果变），且金标是未脱敏建的，**改完尺子量不了**
- B 留证脱敏：产品不变，但存档失复盘价值
- C 两套存档：原文（严权限短期）+ 脱敏（给标注），最干净最贵
- ⚠️ **撞 L3 规范**：内测方案 L3 写「标注对象要和生产输入一致」，脱敏后给标注违反它。上个窗口倾向 C。

**问题 2 · 留多久？** 原始留证 30 天 + **被选进金标 v2 的那批长期**（分开告知用户）。

**问题 3 · 协议怎么说？** 别写「不会泄露」（保证不了），写事实：「语音用字节转写、故事用阿里云分析，两家声明不用于训练；存故事改进产品保留 30 天；被选为评测样本则长期保留，另行告知」。

**问题 4 · 谁读真实故事？** L3 要求双人独立标注=**第二个人要读用户真实生活故事**。有没有第二标注员？没有的话 L3 双人要不要降级（那是「v1 单人单遍欠账不再欠」那笔账）？用户知不知道有第二人读（协议里目前没这句）？

---

## 5. 埋点方案（metric-designer 上个窗口产出，未落库代码）

**前 3 优先事件**（其余见完整方案，在会话记录里，未落盘成文档——可能需要重新让 metric-designer 复述或从头设计）：
1. `match.result`（服务端 `api/matching/route.ts:75-82`）——一个事件交付 L1 前两行半（观察点分布+noMatch+假空率），字段现成、零前置依赖、当天出数
2. `flow.corpus_bound`——「真实故事数」这个分母的定义点，没它算不出漏斗口流失
3. `match.view_rendered`（客户端）——唯一能对上「服务端给了什么 vs 用户真看到什么」

**最大坑**：`flow_id` 透传（贯穿 ASR→restructure→story 的 join key），因为 story_id 在 ASR 和 restructure 之后才诞生（`restructure/page.tsx:111` 才创建 corpus）。这是唯一要「改代码结构」而非「加一行埋点」的东西，且要判断「URL 多个 query 参数算不算用户可见变更」（停机规则第 1 条）——**需产品方判**。
**分母铁律**：所有比率分母用系统外的量（故事/会话/用户数），不用模型输出控制的量。
**第一周只出裸计数与分布，不设任何阈值**（阈值待数据出来后定，台账052）。

---

## 6. 金标两个制品债（台账 056，待产品方处理，改金标=产品方授权+主会话代笔）

`scripts/eval/golden/ranking.v1.json`：
1. **`version` 字段仍是 `"v2"`**，实际已多次改版（v2→v2.1裁决10条→+7条表外补裁）。版本号叫什么由产品方定（上个窗口建议 v2.2）。
2. **`hiddenSampleRate` 键留 `null` 未删**（`run-ranking-score.ts:46` 类型是 `?: number`，null 是类型谎言）。`hiddenSampleRule` 已加。删键前确认代码不崩（run-ranking.ts 写导出侧的同名字段，不是读金标侧，应安全，但要验）。

金标当前分布（已验）：**高11/中19/低67/隐藏51 = 148 labels / 40 故事**。金标 core digest = `ac6ecf0892811c01`（BASELINE 里有防篡改锁）。

---

## 7. 合成红线：冻结在「够用」状态（设计官 9 条记录在案，内测重钉）

BASELINE v3 已 commit（`d1a7f47`）。**已落地生效的**：红线1(召回≥10)/2(首屏可用≥6)/3(未召回=0)/4(noMatch=0)/5(真丢⊆{S051})/6(缺口≤3，但见下)/7A(隐藏冒进⊆∅，故意在轮5/6 FAIL，S074/f9119694 待修) + 八条金标结构断言 + 金标/候选双 digest 防篡改。

**设计官第二轮提案的 9 条（红队判🟡两阻断后的处置），战略拍板后【冻结、不落地、内测重钉】**：
- 红线6 `≤3` 降观察项（红队证它是 flaky gate、并集未收敛）
- 7C 取代红线5（设计官申诉成功、上个窗口主会话认输：`真丢⊆7C`恒成立，冗余的是红线5）
- 红线1 余量2 诚实边界（已在 BASELINE 注释里）
- S074 责任人+期限机制、驳回程序扩到全岗位、缺口5条送金标复审
- **这些都记在这里，内测建金标 v2 后连同全部红线一起重钉，不必现在动。**

**金标复审队列积压**（内测前不必清，但记着）：S051/c2dc0833、S051/6157a9a6+6a65c126（判例漏用）、S061/aee49959——全是「继承自 ranking.v1、从未裁决」的，和今晚翻案的 S056/S046/S047 同类。

---

## 8. subagent + 模型配置现状

- **subagent 链路正常**：上个窗口测了 pipeline-auditor 两轮（opus 配置、gemini 配置），都能正常启动/执行工具/返回，回答质量好。
- **9 个 agent 现在全配 `model: gemini 3 pro`**（含 catch-all `claude.md`）。
- ⚠️ **无法从会话内证明真跑的是 Gemini**：subagent 不知道自己模型、行为和 opus 那轮无差异、`gemini 3 pro`（带空格）可能非法→静默回退到默认 opus 4.5。**要确认 Gemini 真生效**：改标准 ID `gemini-3-pro`、确认环境接了 Google provider、看 Claude Code 日志。这个产品方在调用侧查，你会话内查不到。
- **两个新 agent**（上个窗口建的，已 commit）：`redline-designer`（只提案红线，无 Write/Edit）、`baseline-engineer`（实施红线，human-only 档 guard-golden，金标碰不到）。`guard-golden.sh` 双档位（full/human-only）+ Bash 面拦截。

---

## 9. 今晚的核心教训（别重犯，全是真金白银换的）

1. **顺序：尺子→数据→模型。先金标后 prompt。别拿没校准的尺子调模型。**
2. 别把 n=3/n=6 的常数当实测稳定性（区分「实测恒定」vs「构造性恒真」）。
3. 「稳」必须有主语（稳对随机抖动 ≠ 稳对漂移）。
4. 聚合数字会骗人、复算验证——**但先读 `run-ranking-score.ts` 的现成输出，别从头重算整套指标烧 token**，要抽查再单点看具体条目。
5. 能跑的东西不要推、跑一次验证；**但通道不可信时不做不可逆操作**（今晚就栽在这，停手是对的）。
6. 主会话别当「提案+实施+裁定」三顶帽子一个头：红线走 redline-designer 提案→人拍→baseline-engineer 实施→red-team 审；**金标只有人裁、主会话代笔**。
7. 转述产品方决定必须附**逐字原话**，否则 @recorder 拒绝标「已决策」（已拦过 6 次）。产品方在选择题里「勾选」≠「逐字原话」，recorder 要求分列登记。
8. **该派 subagent 就派**（产品方明确要求）——别所有事自己干。但通道坏时派 subagent 更危险（输出多经一层转述）。

---

## 10. 台账 / HANDOFF 落后，需补

- **DISCUSSION_LOG.md**：只记到今晚前半段（约 067-077）。**后半段全部没记**：订正+digest批次、诊断官首屏虚高结论、PM能否进内测结论、战略拍板、隐私加固、两个新agent、7C申诉、红线6降级……**新窗口应派 @recorder 补入账**（先 `tail` 台账看最后编号）。
- **HANDOFF.md**：停在「§0.5 问三个问题」那版，缺后半段。需更新。

---

## 11. 下一步建议（新窗口开工顺序）

1. **核对**：`git log --oneline -8`（确认今晚 5 commit 在）+ 确认钩子是关的（通道干净）。
2. **产品方拍隐私四问**（§4）——不写代码、最快、解锁留证代码形状。
3. **派 @recorder 补台账后半段**（§10）+ 更新 HANDOFF。
4. **金标两个制品债**（§6）——产品方定 version 叫什么，主会话代笔改+commit。
5. **然后进留证+埋点代码**（①⑤）——现在通道可靠，写代码验证没问题。走 fix-engineer 实施。

**内测不急（产品方原话「内测没那么急，先把问题解决了」）——但方向已定、路已清，按这个顺序推进即可。**
