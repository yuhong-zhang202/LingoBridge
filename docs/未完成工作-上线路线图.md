# 未完成工作 / 上线路线图

> 产品方 2026-07-17 口述排期，主会话记录。决策逐字见 `scripts/eval/loop/DISCUSSION_LOG.md`。

## 部署形态（产品方 2026-07-17 定）
- **生产：Vercel（serverless）**。
- **AI 调用经香港节点中转**（DashScope/字节端点的路由/base_url 需在部署时按 HK 中转配置——见遗留清单）。

## 当前在飞（本会话，待落地）
- **留证 → DB 持久化改造**（fix-engineer 实施中）：迁移 `0020_raw_logs`（llm_raw_logs / asr_raw_logs + RLS + pg_cron 30 天过期）+ `raw-log.ts` 改 Supabase insert + `raw-log-context.ts`（AsyncLocalStorage）+ 7 路由包裹 + 账号删除彻底删两表 + `env` 改 `RAW_LOG_ENABLED`。待审 + 提交。
- **同意页措辞修正（选 A）+ 同意弹窗补 30 天 + bump**（fix-engineer）：待实施 → pm-reviewer 复审 → 提交。

## 上线前路线图（产品方 2026-07-17 排的序）
1. **收尾手头工作**（留证 DB + 同意页）→ 审 + 提交。
2. **检查全部 commit 并 push 到 GitHub**；检查遗漏问题。
3. **新建 3 个评审角色**：
   - 代码架构 & 健康评估员（评代码架构与健康）
   - 产品逻辑评审员（查产品逻辑是否有遗漏）
   - UI/UX 评估员
4. **去技术社区 + GitHub 找可用功能/skill**，完善全部 subagent 技术点，使其更专业。
5. **完善 API 调用费用 Dashboard**（`src/app/dashboard/page.tsx` + `src/app/api/dashboard/` + `src/lib/api-logger.ts`）。**2026-07-18 产品方再调序：Dashboard 排到 3 评审之前。** 早期版本、需较多改动；先审数据接入准确性（已发现 extract/polish/pronounce 未记 usage、matching 估算且漏计一次 LLM、单价汇率会漂）再完善。
6. **隐私前提重评（按"陌生人 200 人"）** —— 2026-07-18 产品方排在 Dashboard 之后、3 评审之前。见下方已知遗留的 🔴 隐私重评项（重评 078/079/080/081/094/095 在 200 陌生人前提下是否成立、同意是否要真捕获、L3 标注谁做、数据保护够不够）。**隐私不派 agent（草案原则：它划真实用户数据处置边界、非技术选型），主会话重评 + 人拍。**
7. **3 个新评审员依次评估**，通过。
8. **生产部署 Vercel + AI 调用转香港节点**。
9. **准备上线**。

> **subagent 升级（2026-07-18 结案）**：升级了 5 个（3 新评审角色 + fix-engineer 接 verify/code-review + pm-reviewer 接 dataviz）。另 7 个（recorder/diagnostician/metric-designer/redline-designer/baseline-engineer/red-team/pipeline-auditor）经主会话逐个评估 = 已在最佳实践线上、无对口 skill、无可加分改动，**产品方拍板：不动**（硬改属净负）。

## 已知遗留 / 待办（穿插在各阶段）
- **部署动作（产品方做）· ⚠️ 迁移必须先于代码上生产库**：迁移 **0018–0022 全部**须在**生产 Supabase** 执行**再**部署代码——尤其 **0022（同意表）未跑 = 同意闸 fail-closed 挡死所有用户**（code-health phase⑦ 发现）。本机已跑 0018–0022；**Vercel 生产库另需再跑一遍**。另：Supabase 启用 **pg_cron 扩展**；Vercel 配 **`RAW_LOG_ENABLED=1`** + **`ADMIN_EMAILS`**（否则线上费用看板无人能进）；一次性删本机旧 `.llm-raw`。
- **香港节点 AI 中转**：确认 `DASHSCOPE_BASE_URL` / 字节 ASR 端点经 HK 的配置（部署时）。
- **法务（2026-07-18 降级·不拦上线）**：产品方定"小产品、200人内测、不确定长期运营，约法务用不着"。200 人内测按 **best-effort 合规**即够（真捕获同意/人读披露/30天最小化/删除权/诚实措辞——工程落地）。**法务 review 留到将来长期运营/商业化/扩规模时再考虑**（含核字节/阿里"不用于训练"条款）。诚实残余风险：不约法务=尽力合规，此规模风险低。
- **🔴 隐私前提重评（2026-07-18 新增·上线前必办）**：内测规模披露为 **200 人、陌生人（非熟人）**。此前隐私决策 078（脱敏读原文）、079（L3 双人=产品方+Claude 无第二标注员）、隐私草案 §3.4 均以"熟人小样本、你信任的人"为前提——**200 陌生人使前提失效**（陌生人真实语音+故事全文的 PIPL/法律责任、真·单独同意 vs 熟人默认、故事被读的知情，均是不同量级）。**内测正式开前，隐私方案须按"陌生人 200 人"整体重评**（含：同意页是否要真捕获同意而非纯展示、L3 双人标注谁来做、数据保护/加密/访问控制是否够）。关联 078/079/080/081/094/095。
- **内测执行（人主导）**：招内测用户、L1/L2/L3 标注流程就位。
- **待内测重估**：加权算分（present 过冲，代码在开关后）、首屏虚高供给缺口（真题材）、`RANKING_ALGO_VERSION` bump 守卫（改排序算法忘 bump 会让老用户吃陈旧冻结档）。
- **技术债 backlog（phase⑦ code-health 提，非上线阻断）**：账号删除加"新 user_id 表必入删除清单"的守卫/测试；RLS 策略的自动化回归验证（现纯人审）；`rankQuestions` 顶层 catch 吞异常过宽（真异常 vs 无匹配遥测不可分）；底部 sheet 类弹窗统一抽 `<Sheet>` 基座补 dialog 语义/focus trap。
- **内测前实测**：匿名同意 → 注册后 uid 是否连续（否则每个注册用户重复弹同意）；0022 未建表时 /api/consent 是否真 500（fail-closed 影响面确认）。
- **评测基准"入选时单独同意"落地**：内测建金标 v2 时做（留证已预留 `retained` 列）。
- **文档对齐三档语义**：低优先（只疼内部）。

## 决策留痕（逐字见台账）
- **故事留存：选 A** —— 故事留到用户自己删/删号即删；仅内部分析日志（留证）保留 30 天。
- **同意弹窗**：补"内部日志留 30 天"+ bump（老用户重看一次）；**评测基准那条仍"入选时另行单独征得"、不进首屏弹窗**。
- **留证**：生产开、全量留、删号彻底删（含金标 retained 批）。
